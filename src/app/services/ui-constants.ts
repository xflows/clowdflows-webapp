let SIGNAL_WIDTH = 40;
let SIGNAL_HEIGHT = 16;
let BOX_WIDTH = 56;
let WIDGET_WIDTH = BOX_WIDTH + 2*SIGNAL_WIDTH;
let CONNECTION_WIDTH = 5;
let ICON_SIDE = 34;

export const UI = {
    signalWidth: SIGNAL_WIDTH,
    signalHeight: SIGNAL_HEIGHT,
    widgetBoxWidth: BOX_WIDTH,
    widgetWidth: WIDGET_WIDTH,
    connectionWidth: CONNECTION_WIDTH,
    iconSide: ICON_SIDE
};